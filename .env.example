# parcelmap uses NO API keys. This file documents optional tunables only.
# Copy to .env if you want to override; otherwise defaults in parcelmap/config.py apply.

# Descriptive identifier sent to free OSM/Esri services (good etiquette).
# PARCELMAP_USER_AGENT="ParcelMapAnnotator/1.0 (your-company@example.com)"
